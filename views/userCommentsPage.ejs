<!DOCTYPE html>

<head>
    <%-include('partials/head')%>
</head>

<body>
    <%-include('partials/navbar')%>
    <%-include('partials/userNavbar')%>
    <ul>
        <% if (user.comments.length > 0) {user.comments.forEach(comment => {%><%-include('partials/commentbox', {comment: comment})%><%})} %>
    </ul>
</body>