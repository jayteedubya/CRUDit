<!doctype=html>
<html>
    <head>
        <%-include('partials/head')%>
    </head>

    <body>
        <%-include('partials/navbar')%>
    </body>
    <ul class="mainlist">
        <% posts.forEach((post) => {%><%-include('partials/postbox', {post: post})%><%}) %>  <!--js code need escaped-->
    </ul>
</html>